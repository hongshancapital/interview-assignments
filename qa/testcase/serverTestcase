get '/toolbox/options'
1、使用非GET方法能不能访问，
2、入参单项为空
3、入参全部为空
4、不带TOKEN访问
5、入参超长检查
6、安全检查：入参SQL注入、双写绕过等
查看返回结果和数据库中数据对应是否正确

post '/toolbox/create'
1、使用非POST方法能不能访问，
2、入参单项为空
3、入参全部为空
4、不带TOKEN访问
5、入参超长检查
6、安全检查：入参SQL注入、双写绕过等
7、入参和数据库中已有数据重复
查看入参是否正确入库

get '/toolbox'
1、使用非GET方法能不能访问，
2、清空数据库中数据，系统是否报错
3、是否正确排序
4、是否正确分页
5、入参超长检查
6、安全检查：入参SQL注入、双写绕过等

以上测试只是检查MESSAGE中内容，另外还要检查：
根据设计文档，针对各种返回码做检查，看是否符合设计中的重定义要求
这三个接口还要根据业务场景进行排列，完成全链路测试
还要加上业务分析中描述的性能要求，进行并发压力测试
接口和前端业务操作对应关系测试
前端展示数据和接口返回数据对应测试（避免传输不不必要数据浪费带宽，也要方式WEB和APP端相同业务操作的细微差异访问不同接口造成服务器负担）
内部接口、外部接口、内部方法设计合理性检查
TOKEN失效测试
网关上传输压缩开关是否按设计打开或关闭
