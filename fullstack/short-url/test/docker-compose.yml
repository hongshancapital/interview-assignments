version: '3'

services:
  db:
    image: postgres:11
    restart: always
    container_name: integration-tests-short-url
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
      POSTGRES_DB: tests