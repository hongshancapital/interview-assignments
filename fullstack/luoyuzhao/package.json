{
  "name": "domain-convert",
  "version": "1.0.0",
  "description": "home-work",
  "scripts": {
    "start": "tsc && node dist/server.js",
    "debug": "tsc && nodemon --inspect dist/server.js",
    "test": "nyc --reporter=text mocha test/**/*.ts --exit"
  },
  "mocha": {
    "require": [
      "ts-node/register"
    ]
  },
  "author": "luoyuzhao",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.3.6",
    "express": "4.17.1",
    "mocha": "^10.0.0",
    "redis": "^4.2.0",
    "reflect-metadata": "^0.1.13",
    "ts-md5": "^1.3.1",
    "typedi": "^0.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "nyc": "^15.1.0",
    "ts-node": "^10.9.1",
    "typescript": "4.3"
  }
}
