export default {
    partition_no: '0', 
    urlId_length: 5,
    db_size: 900000000,
    redisurl_map: {
        '00': 'redis://:pwd@host:port',
        '01': 'redis://:pwd@host:port/dbnum'
    }
}