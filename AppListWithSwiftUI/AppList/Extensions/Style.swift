//
//  Style.swift
//  AppList
//
//  Created by 王宁 on 2022/4/1.
//

import UIKit
import SwiftUI

struct Styles {
    struct Colors {
        static var background: Color {
            let color = UIColor.init(named: "background")!
            return Color(color)
        }
        
        static var text: Color {
            let color = UIColor.init(named: "text")!
            return Color(color)
        }
        
        static var white: Color {
            let color = UIColor.init(named: "white")!
            return Color(color)
        }
        
        static var clear: Color {
            let color = UIColor.init(named: "clear")!
            return Color(color)
        }
        
        static var border: Color {
            let color = UIColor.init(named: "border")!
            return Color(color)
        }
        
        static var textGray: Color {
            let color = UIColor.init(named: "textGray")!
            return Color(color)
        }
    }
}
