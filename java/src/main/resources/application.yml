server:
  port: 8080
spring:
  application:
    name: short-domain
logging:
  level:
    root: info
domain:
  # 短域名的前缀
  #  shortHost: http://127.0.0.1:8080
  #服务器最大支持的短域名容量 根据服务器的配置实际来调整这个值
  maxCapacity: 1000

