server:
  port: 12345
  tomcat:
    basedir: logs
    accesslog:
      pattern: '%t %m %s %v %U'
      enabled: true
      directory: access-logs
      buffered: true
      file-date-format: .yyyy-MM-dd

