1. 生成的短链接必须唯一，这一点UUID和Snowflake算法都可以实现，但长度不符合要求，存储和查询有缺陷。
2. 自增ID也可以保证唯一，不过此算法可以别人也可以获取到你的原始地址。
3. hash算法，此方法生成的url没有规律，有很小的几率发生碰撞，基本可以忽略。
4. 随机数，在伪随机的情况下，发生碰撞的几率比较大。

所以最后选择了hash算法，在真的出现碰撞的时候，用随机字符做为候选。
