设计思路;
   1 长域名转短域名算法：
          系统自增Id作为种子，和长连接建立映射。 因为短域名长度是8，所以需要把自增的id
压缩为8位，考虑到转为62进制，有两个好处；1 长度边小，2 不含/.等特殊字符。
      (Long类型2的64次方大于62的8次方)
   2  存储
        直接存在内存中，因为要考虑内存溢出，所以一定要有淘汰策略，考虑用linkedhashmap做lru。
        初始设置容量大小，超过则删除最远使用的数据。
   3  浪费
         考虑到相同的长连接如果映射不同短链接有浪费，则缓存访问过的长连接。当热的长连接访问的时候，这个可以一定程度上减少生成短链接，减少浪费。容量大小要小于lru缓存的大小，不然数据量大的时候是空间的浪费。
   